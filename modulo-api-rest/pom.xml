<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>co.unicauca.saberpro</groupId>
        <artifactId>banco-preguntas-saberpro</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>modulo-api-rest</artifactId>
    <packaging>jar</packaging>

    <name>Modulo API REST (Taller 6)</name>
    <description>
        Microservicio REST (Spring Boot + Spring Data JPA) que expone el CRUD
        de preguntas del banco. Reutiliza el dominio de modulo-preguntas
        (Question, QuestionService) y le agrega un adaptador JPA como
        implementacion del puerto QuestionRepository, de modo que el dominio
        no depende de JPA ni de Spring.
    </description>

    <properties>
        <spring-boot.version>3.5.16</spring-boot.version>
    </properties>

    <!--
        Se importa el BOM de Spring Boot en vez de heredar de
        spring-boot-starter-parent, porque este modulo ya hereda del pom
        padre del proyecto (monolito modular).
    -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>co.unicauca.saberpro</groupId>
            <artifactId>modulo-preguntas</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Spring MVC necesita los nombres de parametros (@PathVariable sin nombre). -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <parameters>true</parameters>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot.version}</version>
                <configuration>
                    <mainClass>co.unicauca.saberpro.api.ApiRestApplication</mainClass>
                    <finalName>banco-preguntas-api</finalName>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
